24.04

Lilo
Grub
Grub2

Die allgemein wichtigen Ablageorte des Betriebssystemkerns und der Ramdisk sollten bekannt sein. Daher hier auf der 1. Seite.
/boot/vmlinuz-*         Der Betriebssystemkern
/boot/initrd.img-*      Die Ramdisk (mit zus¨atzlichen Treibern)
/vmlinuz                Verknupfung mit dem Kern ¨
/initrd.img             Verknupfung mit der Ramdisk


wichtige Dateien und Pfade
/boot/grub  	            Das Konfigurationsverzeichnis
/boot/grub/menu.lst         Die Hauptkonfigurationsdatei
/boot/grub/grub.conf        Eine Verknupfung auf die Hauptkonfiguration ¨
/etc/grub.conf              Eine Verknupfung auf die Hauptkonfiguration ¨
grub-md5-crypt              Befehl zur Erstellung eines MD5-Hash-Passwortes
grub-install                Befehl zur Installation des GRUB


Beispiel                Grub

default 0
hiddenmenu
title   	                 Debian GNU/Linux
root        (hd0,0)
kernel      /vmlinuz root=/dev/sda1 ro quiet splash
initrd      /initrd.img


title       Fedora Red Hat Workstation
root        (hd1,0)
kernel      /vmlinuz root=LABEL=/ ro quiet rhgb acpi=off
initrd      /initrd.img


wichtige Dateien und Pfade Grub 2
/boot/grub/                 Das Konfigurationsverzeichnis
/boot/grub/grub.cfg         generierte Hauptkonfigurationsdatei
/boot/grub/device.map       Zuordnungen der Ger¨ate (z.B. hd0 zu sda)
/etc/default/grub           Konfigurationsdatei fur die manuelle Konfiguration ¨
/etc/grub.d/                Skripte zur Generierung der grub.cfg
update-grub                 Aktualisieren der Konfiguration
grub-install                Installation des GRUB
grub-mkconfig               Erstellt die Konfiguration grub.cfg




/etc/default/grub

GRUB DEFAULT=0
# timeout in Sekunden
GRUB TIMEOUT=10
GRUB DISTRIBUTOR=‘lsb release -i -s 2> /dev/null | echo Debian‘
# Kernelparameter
GRUB CMDLINE LINUX DEFAULT=‘quiet splash‘
GRUB CMDLINE LINUX=‘‘

#	update-grub
# 	grub(2)-install /dev/sda




Grub 2 überlisten

NACH linux -- init=/bin/bash --vor "net.ifname"

System startet ohne passwort aber als "nur lesen"

muss neu in rw gemountet werden

mount -o remount -o ????????
